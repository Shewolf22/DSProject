#include<time.h>
#include <stdio.h>
#define finerate 5  //fine rate per day ...rs-5
 
 char* currtime();
 float finecalc(int,int);
 
 int main()
{ 
  char* c_t;
  float f1;
  c_t=currtime();                          //current date and time stored in c_t
  printf("\n%s",c_t);
  issuedate=  //get the issue date here from book data
  curdate=datecalc();
  f1=finecalc(issuedate,currdate);            //fine calculation fn called
  
    return 0;
}

char* currtime()                              //fn gives current day_date_time_year
{
   time_t ct;
   
   ct=time(NULL);
   char* cts;
   cts=ctime(&ct);
   printf("%d",date-date1);
   return cts;  
}

int datecalc()                                //gives only date---to b used for fine calculation
{
    time_t date;
    date=time(NULL)/86400;
    return date;
    }

float finecalc(issuedate,curdate)               // the fine calculation function
{
    float fine;
   int no.of.days=curdate-issuedate;
   fine=finerate*no.of.days;
   return fine;
}    
    
    
    
    
    
